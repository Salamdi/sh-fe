const { noteQuery } = require('./NoteQuery');
const { userQuery } = require('./UserQuery');
const { jokeQuery } = require('./JokeQuery');

module.exports = {
  noteQuery,
  userQuery,
  jokeQuery,
};
